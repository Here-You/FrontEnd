import styled from 'styled-components';

const ContentsContainer = styled.div`
  width: 300px;
  margin-bottom: 23px;
`;

const PreviewContainer = styled.div`
  display: flex;
  overflow-x: auto;
  gap: 15px;
`;

export { ContentsContainer, PreviewContainer };
